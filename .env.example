# =====================================================
# Idol API 環境変数設定例
# cp .env.example .env してから値を編集してください
# =====================================================

# --- MongoDB 設定 ---
# ローカル開発: mongodb://localhost:27017
# Docker Compose: mongodb://admin:<password>@mongodb:27017/?authSource=admin
# MongoDB Atlas: mongodb+srv://<user>:<password>@<cluster>.mongodb.net
MONGODB_URI=mongodb://admin:password@localhost:27017/?authSource=admin
MONGODB_DATABASE=idol_database

# --- Docker Compose 用 MongoDB 認証 ---
MONGO_USERNAME=admin
MONGO_PASSWORD=change_me_in_production

# --- サーバー設定 ---
SERVER_PORT=8081

# --- 実行モード ---
# ローカル開発: debug
# 本番環境: release
GIN_MODE=debug

# --- CORS 設定 ---
# カンマ区切りで許可するオリジンを指定
# 本番環境ではフロントエンドのURLのみを許可すること
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# --- APIキー認証設定 ---
# スコープ階層: admin ⊇ write
# Authorization: Bearer <KEY> ヘッダーで送信
#
# write スコープ: コンテンツの作成・更新・削除（POST/PUT/DELETE）
# 本番環境では十分にランダムな値を設定すること（例: openssl rand -hex 32）
WRITE_API_KEY=change_me_to_a_secure_random_write_key
#
# admin スコープ: 削除申請管理など運営者専用操作
# admin キーは write スコープでも使用可能
# 本番環境では 32 文字以上必須（例: openssl rand -hex 32）
ADMIN_API_KEY=change_me_to_a_secure_random_admin_key

# --- OIDC 認証（idol-auth / Ory Hydra 連携、空の場合は API キー認証のみ）---
# Hydra の公開 URL（例: https://auth.example.com）
# idol-auth の HYDRA_BROWSER_URL に合わせること
OIDC_ISSUER=https://auth.example.com
# idol-api のリソースサーバー識別子（例: https://api.idol.example.com）
# Hydra のクライアント設定の audience と一致させること
OIDC_AUDIENCE=https://api.idol.example.com

# --- レート制限 ---
# 1秒あたりのリクエスト数上限（デフォルト: 10）
# インメモリ実装のため、水平スケール時はレプリカ数で割った値を設定すること
# 例: 3レプリカなら RATE_LIMIT_RPS=4 程度に下げる
RATE_LIMIT_RPS=10
# バースト許容数（デフォルト: 20）
RATE_LIMIT_BURST=20

# --- 公開POST系の追加レート制限 ---
# 投稿・削除申請・外部Webhook受信に追加適用する低レート制限
PUBLIC_MUTATION_RATE_LIMIT_RPS=0.2
PUBLIC_MUTATION_RATE_LIMIT_BURST=3
